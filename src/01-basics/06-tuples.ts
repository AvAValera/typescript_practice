const pair1: [string, string][] = [];
pair1.push(["key", "value"]);

const arrType: [string, number, boolean] = ["name", 12, true];

const arrType2: [string, number, {a: string, b: number}][] = [];
arrType2.push(["name", 15, {a:"name2", b:16}])